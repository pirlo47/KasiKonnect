from flask import Flask, redirect, url_for, session
import os

app = Flask(__name__)
app.secret_key = os.getenv("SECRET_KEY") 

@app.route('/')
def home():
    return "Welcome to KasiKonnect"

if __name__ == "__main__":
    app.run(debug=True)
